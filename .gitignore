# Captures de SleepMapper — données personnelles, dépôt public
# Le relevé écrit (docs/sleepmapper/README.md) reste versionné, les images non.
docs/sleepmapper/*.png
docs/sleepmapper/*.jpg
docs/sleepmapper/*.jpeg

# Canvas publiés : ~2,5 Mo chacun (payload de l'éditeur). Se régénèrent depuis
# les .dc.html et canvas.json par le helper du skill design.
design/*.html
!design/*.dc.html

# Expo / Node
node_modules/
.expo/
dist/
web-build/
expo-env.d.ts
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.pem
.metro-health-check*
npm-debug.*
.env*.local
*.tsbuildinfo
# dossiers natifs générés par `expo prebuild` / EAS
/ios
/android
